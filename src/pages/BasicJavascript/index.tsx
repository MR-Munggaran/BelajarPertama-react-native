import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function index() {
  return (
    <View style={{padding: 20}}>
      <Text style={{textAlign:'center'}}>Materi Javascript</Text>
    </View>
  )
}

const styles = StyleSheet.create({})